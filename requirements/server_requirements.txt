# KidBot — PC Server dependencies
fastapi>=0.111.0
python-multipart>=0.0.9
uvicorn[standard]>=0.30.0
faster-whisper>=1.0.0
ollama>=0.3.0
kokoro-onnx>=0.4.0
soundfile>=0.12.1
# ffmpeg must also be installed system-wide: winget install ffmpeg
# Ollama must be installed system-wide: https://ollama.com/download
