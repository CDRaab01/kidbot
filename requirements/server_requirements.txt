# KidBot — PC Server dependencies
fastapi>=0.111.0
python-multipart>=0.0.9
uvicorn[standard]>=0.30.0
faster-whisper>=1.0.0
openai>=1.0.0
kokoro-onnx>=0.4.0
soundfile>=0.12.1
requests>=2.32.0
slowapi>=0.1.9
# ffmpeg must also be installed system-wide: winget install ffmpeg
# LM Studio must be installed: https://lmstudio.ai/ — load Gemma 4 E4B model and enable the local server
